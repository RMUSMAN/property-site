@{

      var db=Database.Open("StarterSite");
    var id = UrlData[0] ?? "14";
    var sql = @"SELECT * FROM RENT WHERE ID = @0";
    var item = db.QuerySingle(sql, id);
    Json.Write(item, Response.Output);
 
}

